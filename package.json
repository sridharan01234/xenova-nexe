{
  "name": "context-provider",
  "version": "1.0.0",
  "description": "A Node.js CLI application that converts workspaces into vector embeddings using Xenova/Transformers",
  "main": "src/index.js",
  "bin": {
    "context-provider": "./src/index.js"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node src/index.js",
    "build": "node build.js",
    "build:force": "NEXE_BUILD=true node build.js",
    "build:win": "nexe src/index.js -o dist/context-provider.exe -t windows-x64-18.17.0 --build",
    "build:mac": "nexe src/index.js -o dist/context-provider-mac -t macos-x64-18.17.0 --build",
    "build:all": "npm run build:force && npm run build:win && npm run build:mac",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "cli",
    "vector-embeddings",
    "transformers",
    "workspace",
    "nexe"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@xenova/transformers": "^2.17.2",
    "commander": "^11.1.0",
    "fs-extra": "^11.2.0",
    "glob": "^10.3.10",
    "ignore": "^5.3.0",
    "mime-types": "^2.1.35",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "nexe": "^4.0.0-rc.6"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "nexe": {
    "target": "linux-x64-18.17.0",
    "build": true,
    "output": "dist/context-provider",
    "excludes": [
      "node_modules/@xenova/transformers/dist/models/**/*"
    ]
  }
}
